# Production
paho-mqtt>=2.0.0
numpy>=1.26.0
influxdb-client>=1.40.0

# Manual state publisher for VR testing (services/state_api.py)
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.0.0

# Research / legacy engines (imported lazily; only needed with the
# biofizic/legacy toggles on — see biofizic/legacy/toggles.py)
scipy>=1.11.0          # PPG band-pass + peak detection (FAZA D), WESAD R-peaks
scikit-learn>=1.3.0    # WESAD RandomForest (train/train_wesad.py)
joblib>=1.3.0          # load the trained WESAD model
neurokit2>=0.2.0       # NK shadow validation in services/test_engine.py + HRV
                       # reference oracle in tests/test_reference_hrv.py

# Dev / test
pytest>=8.0.0
